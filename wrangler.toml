name = "gtfs-collector"
main = "src/worker.js"
compatibility_date = "2024-09-01"

[[r2_buckets]]
binding = "BUCKET"
bucket_name = "gtfs-lviv"

[triggers]
crons = ["* 1-21 * * *"]  # every minute UTC 01–21; runtime guard (Kyiv <4 skip) gates the 04:00 start across DST
